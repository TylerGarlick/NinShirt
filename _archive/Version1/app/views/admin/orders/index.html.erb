<h1>Orders</h1>
<table>
	<thead>
		<tr>
			<th>Id</th>
			<th>Date</th>
			<th>Total</th>
		</tr>
	</thead>
	<tbody>
	<% @orders.each do |order| %>
		<tr>
			<td><%= order.id %></td>
			<td><%= order.created_at %></td>
			<td><%= order.total %></td>
		</tr>
	<% end %>
	</tbody>
</table>